# Merger

When new evidence arrives:

- update only the affected layers unless the archive crosses a state boundary
- preserve prior conclusions when the new evidence is additive
- surface disputes instead of silently overwriting them
- append formal corrections to `corrections.md`

Every successful merge must end with a new archive snapshot.
